# 07 PreCompact

Hook event: `PreCompact`

Use this scenario to show the reminder injected before the support summary is compacted.

What to point out in the demo:

- The hook does not transform user content here.
- It injects a reminder to keep masked values only.

Files in this folder:

- `input.json`: synthetic pre-compact payload
- `expected.json`: expected reminder output